openapi: 3.0.0
info:
  version: 2.0.0
  title: NSM Configuration Server APIs
  description: |
    NSM Configuration service

    One read operation. The response is a struct of per-topic sub-objects and
    presence is the discriminator: NSM fills the sub-object of every topic the
    request named and leaves the rest absent. An unknown topic is a 400; an
    absent or empty topic list means every topic.

    The read is conditional. The request carries the version the consumer has
    already applied; if that is the version NSM would render, the answer is
    `unchanged` and no sub-objects. A conditional request in all but name, with
    the content hash as the ETag - the difference being that it is expressed in
    the body rather than in the status line.

    The request's topic list is the only selector. NSM does not scope the answer
    by caller: the SBI server terminates mutual TLS against the deployment's CA,
    so mesh membership is the trust boundary and there is no second one inside
    it. Across a cluster boundary NSM sees the gateway's certificate rather than
    the caller's anyway.

    Nothing here allocates. Every topic renders as a pure function of what NSM
    holds, so a consumer may repeat this call as often as it likes and two
    replicas answer it identically. The exclusive allocations - an AMF pointer,
    an SMF's UE-IP segments - are their own operations in the resource
    allocation API.

    © 2024, ETRI
    All rights reserved.
servers:
  - url: '{apiRoot}/nsm-conf/v1'
    variables:
      apiRoot:
        default: https://example.com
        description: apiRoot as defined in clause 4.4 of 3GPP TS 29.501.

paths:
  /configuration:
    post:
      operationId: GetConfiguration
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigurationRequest'
        required: true

      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigurationResponse'

        '400':
          description: Bad Request
          content:
            application/problem+json:
              schema:
                $ref: 'TS29571_CommonData.yaml#/components/schemas/ProblemDetails'

        '500':
          description: Internal Server Error
          content:
            application/problem+json:
              schema:
                $ref: 'TS29571_CommonData.yaml#/components/schemas/ProblemDetails'

components:
  securitySchemes:
  schemas:
    ConfigurationTopic:
      description: |
        A unit of configuration that is rendered, held and applied on its own.
        A topic is a rendered view rather than one of NSM's tables: NSM decides
        how to compute it, the consumer keeps asking for the same one.
      type: string
      enum:
        - slicing
        - session-management
        - user-plane
        - udm
        - udr
        - sepp

    ConfigurationRequest:
      description: |
        The topics the consumer holds, and the version it holds them at.
      type: object
      properties:
        topics:
          type: array
          items:
            $ref: '#/components/schemas/ConfigurationTopic'
        knownVersion:
          description: |
            The version the consumer has already applied, or empty if it has
            none. Empty is what boot looks like, which is why boot needs no
            separate path: an NF that starts with nothing is an NF whose known
            version is empty, and it gets everything.

            Send only a version that was **applied**. A consumer that refused an
            answer - because adopting it would have left it unable to serve -
            still holds the version before it, and sending the one it refused
            would have NSM answer "unchanged" forever while the consumer sat on
            configuration it had already rejected.

            It is compared against the version of what this request would render,
            so it is meaningful only for a request naming the same topics. Every
            consumer asks for the same set every time.
          type: string

    ConfigurationResponse:
      description: |
        One sub-object per requested topic. A topic that was not asked for is
        absent; a topic that was asked for is present even when what it renders
        is empty, so a consumer can tell "nothing configured" from "not asked
        for". Whether an empty answer is *enough* is the consumer's judgement,
        never NSM's - an HTTP status cannot express it.
      type: object
      properties:
        version:
          description: |
            The content hash of everything else in this body: change nothing and
            it does not move, change what this consumer would receive and it
            does.

            A hash rather than a counter, and the difference is what it buys. It
            needs no persistence, so NSM restarting produces the same version and
            nobody re-pulls. It is computed per request from what NSM holds, so
            two replicas answer identically without talking to each other. It
            needs no dependency map, because the question "would this consumer
            get something different" is answered by the answer itself rather than
            by NSM knowing which topics a data network affects. And it is exact:
            an edit that does not change what *this* consumer would receive
            leaves this version alone, whatever the operator touched.

            Hashes do not order - you cannot tell newer from older - and nothing
            needs them to. A consumer never applies a version; it re-reads, and a
            read always returns what NSM currently holds.

            It covers exactly the topics that were asked for, so it is only
            comparable across requests that asked for the same ones. Every
            consumer asks for the same set every time.
          type: string
        unchanged:
          description: |
            True when the request's knownVersion is the version NSM would have
            rendered, in which case every sub-object below is absent and there is
            nothing for the consumer to do.

            It is stated rather than inferred, and that is the whole reason it
            exists. Presence is the discriminator for the topics, so "asked for
            and absent" already means "NSM holds nothing for that topic" - a real
            answer a consumer checks for and reports. A body with everything
            absent could not also mean "nothing moved" without those two
            colliding, and the one that would lose is the diagnosable one.

            The natural encoding is a 304 with no body at all. The generator this
            API is produced by emits exactly one success response per operation,
            so a second status code would reach the consumer through its default
            branch as a bare error. A field costs forty bytes against the few
            kilobytes it replaces.
          type: boolean
        slicing:
          $ref: '#/components/schemas/SlicingConfiguration'
        smf:
          $ref: '#/components/schemas/SessionManagementConfiguration'
        upf:
          $ref: '#/components/schemas/UserPlaneConfiguration'
        udm:
          $ref: '#/components/schemas/UdmConfiguration'
        udr:
          $ref: '#/components/schemas/UdrConfiguration'
        sepp:
          $ref: '#/components/schemas/SeppConfiguration'
      required:
        - version

    AmfSetConfiguration:
      description: |
        One AMF set: which region it is in and which slices it serves.

        region is its own field rather than the leading part of setId, because
        a consumer that had to split "10-100" on the hyphen would be parsing a
        composed key to take a decision.
      type: object
      properties:
        setId:
          type: string
        region:
          type: string
        slices:
          type: array
          items:
            $ref: 'TS29571_CommonData.yaml#/components/schemas/Snssai'
      required:
        - setId
        - region

    SlicingConfiguration:
      description: |
        The slice/AMF-set table, the serving slice list and the roaming peers.

        Three consumers filter this one view rather than asking for three:
        DAMF maps a slice onto an AMF set, PRAN takes the union of the slices of
        the sets in its own region, an AMF takes its own set's slices. All three
        change together whenever an operator edits an AMF set, so they are one
        topic - a consumer never sees half an edit.
      type: object
      properties:
        amfSets:
          type: array
          items:
            $ref: '#/components/schemas/AmfSetConfiguration'
        slices:
          type: array
          items:
            $ref: '#/components/schemas/SliceConfiguration'
        plmnPeers:
          type: array
          items:
            $ref: '#/components/schemas/HomePlmnConfiguration'
      required:
        - amfSets

    IpFamily:
      description: The address family an address space allocates from.
      type: string
      enum:
        - IPV4
        - IPV6

    UeIpSource:
      description: |
        Where a UE address on this space comes from. Only POOL is allocated by
        the SMF out of segments NSM leases it; the other three are allocated
        elsewhere and NSM leases no segment for them, but the space still
        carries the DNS, P-CSCF and MTU the session needs.
      type: string
      enum:
        - POOL
        - DHCP
        - DN_AAA
        - STATIC

    AddressSpaceConfiguration:
      description: |
        One address space: a set of prefixes in one routing domain, with the
        length NSM leases in and the length a session is given out of it.

        segmentLength is the size of the chunk NSM leases to a single SMF
        instance; allocationLength is what one PDU session gets - 32 for IPv4,
        64 for IPv6 SLAAC, shorter for prefix delegation. Both are explicit
        because an index into a partition is meaningless without the divisor,
        and the divisor is exactly the thing an operator edit changes.

        dnai names the point of access these prefixes are routed to. An address
        from a prefix routed to one N6 attachment is useless on a session
        anchored elsewhere, so it is part of the space's identity rather than an
        attribute of it. Empty means the space is reachable at every DNAI its
        network instance declares.
      type: object
      properties:
        family:
          $ref: '#/components/schemas/IpFamily'
        dnai:
          type: string
        ipIndex:
          type: integer
          format: int32
        source:
          $ref: '#/components/schemas/UeIpSource'
        prefixes:
          type: array
          items:
            type: string
        segmentLength:
          type: integer
          format: int32
        allocationLength:
          type: integer
          format: int32
        exclusions:
          type: array
          items:
            type: string
        dhcpServer:
          type: string
      required:
        - family
        - source

    NetworkInstanceConfiguration:
      description: |
        One routing domain under a data network, and the identity that makes UE
        addresses unique: two addresses may repeat across network instances and
        must not repeat within one. It is what the UPF is told on the PDI and
        the FAR, and it is not a selection mechanism - the SMF must already know
        which instance a session belongs to before it builds the rules.

        dnais are the points of access at which this instance is reached; the
        SMF selects an anchor by DNAI and then tells the UPF the instance.
      type: object
      properties:
        name:
          type: string
        dnais:
          type: array
          items:
            type: string
        addressSpaces:
          type: array
          items:
            $ref: '#/components/schemas/AddressSpaceConfiguration'
        dns:
          $ref: 'TS29571_CommonData.yaml#/components/schemas/IpAddr'
        pcscf:
          $ref: 'TS29571_CommonData.yaml#/components/schemas/IpAddr'
        mtu:
          type: integer
          format: int32
      required:
        - name

    DataNetworkConfiguration:
      description: |
        A data network as an SMF sees it: its name, the slices that reach it,
        and the routing domains under it. snssais is carried so the SMF filters
        locally rather than NSM filtering per caller.
      type: object
      properties:
        name:
          type: string
        snssais:
          type: array
          items:
            $ref: 'TS29571_CommonData.yaml#/components/schemas/Snssai'
        networkInstances:
          type: array
          items:
            $ref: '#/components/schemas/NetworkInstanceConfiguration'
      required:
        - name

    SessionManagementConfiguration:
      description: |
        Everything an SMF reads. It allocates nothing: the segments this SMF may
        hand addresses out of come from AllocateIpSegments, so this view has no
        side effect and may be re-read at any time.
      type: object
      properties:
        transportNetworks:
          type: array
          items:
            type: string
        dataNetworks:
          type: array
          items:
            $ref: '#/components/schemas/DataNetworkConfiguration'
        slices:
          type: array
          items:
            $ref: '#/components/schemas/SliceConfiguration'

      required:
        - dataNetworks
        - transportNetworks
        - slices

    DataNetworkInfo:
      description: |
        A data network as a UPF sees it: the slices that reach it and the
        prefixes whose downlink traffic belongs in a GTP tunnel. A list rather
        than one CIDR, because adding a prefix to an address space must not
        renumber anything.
      type: object
      properties:
        name:
          type: string
        snssais:
          type: array
          items:
            $ref: 'TS29571_CommonData.yaml#/components/schemas/Snssai'
        prefixes:
          type: array
          items:
            type: string
      required:
        - name

    UserPlaneConfiguration:
      description: Everything a UPF reads.
      type: object
      properties:
        transportNetworks:
          type: array
          items:
            type: string
        dataNetworks:
          type: array
          items:
            $ref: '#/components/schemas/DataNetworkInfo'
        slices:
          type: array
          items:
            $ref: '#/components/schemas/SliceConfiguration'

      required:
        - dataNetworks
        - transportNetworks
        - slices

    SliceConfiguration:
      description: Slice configuration
      type: object
      properties:
        id:
          $ref: 'TS29571_CommonData.yaml#/components/schemas/Snssai'
        dnnList:
          type: array
          items:
            type: string
      required:
        - id

    HomePlmnConfiguration:
      description: Home network configuration
      type: object
      properties:
        id:
          $ref: 'TS29571_CommonData.yaml#/components/schemas/PlmnId'
        slices:
          type: array
          items:
            $ref: 'TS29531_Nnssf_NSSelection.yaml#/components/schemas/MappingOfSnssai'
        sepps:
          type: array
          items:
            type: string
      required:
        - id

    SuciProfile:
      description: SUCI profile
      type: object
      properties:
        protectionScheme:
          type: integer
          format: int16
        publicKey:
          type: string
        privateKey:
          type: string
      required:
        - protectionScheme
        - publicKey
        - privateKey

    UdrConfiguration:
      description: Udr configuration
      type: object
      properties:
        dbName:
          type: string
        authSub:
          type: string
        amSub:
          type: string
        smSub:
          type: string
        smfSel:
          type: string
        url:
          type: string
        mock:
          type: boolean
      required:
        - dbName
        - authSub
        - url

    UdmConfiguration:
      description: Udm configuration
      type: object
      properties:
        udr:
          $ref: '#/components/schemas/UdrConfiguration'
        suciProfiles:
          type: array
          items:
            $ref: '#/components/schemas/SuciProfile'
      required:
        - udr
        - suciProfiles

    SeppConfiguration:
      description: Sepp configuration
      type: object
      properties:
        plmnList:
          type: array
          items:
            $ref: '#/components/schemas/HomePlmnConfiguration'
      required:
        - plmnList
